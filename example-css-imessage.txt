/* iMessage 风格示例：小尾巴像 iMessage 那样从气泡边缘延伸出来 */
.sc-bubble {
  position: relative !important;
  border-radius: var(--bubble-radius, 22px) !important;
  padding: 12px 20px 14px 18px !important;
  box-shadow: none !important;
  line-height: 1.45 !important;
  font-weight: 400 !important;
}
.sc-bubble::after {
  content: '';
  position: absolute;
  bottom: 0;
  width: var(--bubble-tail-width, 10px);
  height: var(--bubble-tail-height, 14px);
  z-index: -1;
  background: var(--bubble-color);
  clip-path: polygon(0 0, 100% 50%, 0 100%);
}
.sc-user {
  background: #007AFF !important;
  color: #ffffff !important;
}
.sc-user::after {
  right: calc(var(--bubble-tail-offset, 6px) * -1);
}
.sc-sys {
  background: #E9E9EB !important;
  color: #000000 !important;
}
.sc-sys::after {
  left: calc(var(--bubble-tail-offset, 6px) * -1);
  transform: scaleX(-1);
  transform-origin: left center;
}

/* 底部输入栏：白底、输入框带浅灰背景、蓝色发送按钮 */
.sc-chat-input-bar {
  background: #ffffff !important;
  padding: 8px 12px !important;
  gap: 6px !important;
}
.sc-chat-input-plus,
.sc-chat-input-continue {
  display: none !important;
}
.sc-chat-input-emoji {
  order: -1 !important;
  color: #8E8E93 !important;
}
.sc-chat-input-field {
  background: #F2F2F7 !important;
  border-radius: 20px !important;
  height: 38px !important;
  padding: 0 14px !important;
  color: #1C1C1E !important;
}
.sc-chat-input-send {
  background: #007AFF !important;
  box-shadow: none !important;
  opacity: 1 !important;
  transform: none !important;
}

/* 悬浮球：把加号和继续说收进右下角圆形菜单 */
.sc-chat-input-fab {
  display: flex !important;
  position: fixed !important;
  right: 14px !important;
  bottom: 76px !important;
  z-index: 40 !important;
  flex-direction: column !important;
  align-items: center !important;
  gap: 10px !important;
}
.sc-chat-input-fab-toggle {
  width: 46px !important;
  height: 46px !important;
  border-radius: 50% !important;
  background: #007AFF !important;
  color: #ffffff !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.18) !important;
  border: none !important;
}
.sc-chat-input-fab-menu {
  display: flex !important;
  flex-direction: column !important;
  align-items: center !important;
  gap: 10px !important;
}
.sc-chat-input-fab-item {
  width: 42px !important;
  height: 42px !important;
  border-radius: 50% !important;
  background: #ffffff !important;
  color: #007AFF !important;
  border: 1px solid #E5E5EA !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08) !important;
}
